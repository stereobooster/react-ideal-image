import React, { type FC } from "react";
import Media from "../Media";
import icons from "../icons";
import theme from "../theme";

const MediaWithDefaults: FC<MediaProps> = (props) => <Media {...props} />;

export default MediaWithDefaults;
